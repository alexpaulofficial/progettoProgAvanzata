version: '3.1'
services:
  db:
    image: postgres:latest
    env_file:
      - .env
    volumes:
      - ./data/seed.sql:/docker-entrypoint-initdb.d/seed.sql

  app:
    build: .
    restart: always
    ports:
      - 3000:3000
    depends_on:
      - db
    env_file:
      - .env
